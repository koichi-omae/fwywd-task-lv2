import { PageHeader } from '@/component/project/PageHeader';
import { Todo } from '@/component/project/Todo';

const Home: React.FC = () => (
  <>
    <PageHeader />
    <Todo />
  </>
);

export default Home;
